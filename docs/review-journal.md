# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its trading systems focus without claiming live deployment or external usage.

## Cases

- `baseline`: `spread pressure`, score 148, lane `ship`
- `stress`: `fill risk`, score 224, lane `ship`
- `edge`: `portfolio drift`, score 190, lane `ship`
- `recovery`: `quote width`, score 149, lane `ship`
- `stale`: `spread pressure`, score 203, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
